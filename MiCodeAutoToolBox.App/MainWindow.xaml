<Window x:Class="MiCodeAutoToolBox.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:MiCodeAutoToolBox.App.ViewModels"
        xmlns:local="clr-namespace:MiCodeAutoToolBox.App.Controls"
        mc:Ignorable="d"
        Title="ZenithKit" Height="620" Width="1080"
        Background="{StaticResource PrimaryBackground}">

    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{StaticResource HeaderBackground}" Height="56" Padding="16,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Zenith" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimary}" Margin="0,0,8,0"/>
                    <TextBlock Text="Kit" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                </StackPanel>
                <TextBlock Grid.Column="1" Text="日常效率工具合集" Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,0">
                    <StackPanel.Resources>
                        <Style TargetType="Button">
                            <Setter Property="Padding" Value="12,6" />
                            <Setter Property="Margin" Value="8,0,0,0" />
                            <Setter Property="Background" Value="#2a3346" />
                            <Setter Property="BorderBrush" Value="Transparent" />
                        </Style>
                    </StackPanel.Resources>
                    <Button Content="设置"/>
                    <Button Content="关于"/>
                </StackPanel>
            </Grid>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="260"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="{StaticResource PanelBackground}" Padding="12">
                <StackPanel>
                    <TextBlock Text="模块" Foreground="{StaticResource TextMuted}" Margin="4,0,0,8" FontWeight="Bold"/>
                    <ListBox ItemsSource="{Binding Modules}"
                             SelectedItem="{Binding SelectedModule}"
                             DisplayMemberPath="Name"
                             Margin="0,0,0,8"
                             MinHeight="400" />
                    <local:StorageSwitcher Margin="0,8,0,0" />
                </StackPanel>
            </Border>

            <Grid Grid.Column="1" Background="{StaticResource PrimaryBackground}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Background="{StaticResource PanelBackground}" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="1" Margin="12" Padding="14" CornerRadius="6">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0">
                        <StackPanel.Resources>
                            <Style TargetType="StackPanel">
                                <Setter Property="Margin" Value="12,0,0,0" />
                            </Style>
                        </StackPanel.Resources>
                        <StackPanel>
                            <TextBlock Text="模块" Foreground="{StaticResource TextMuted}" FontSize="12"/>
                            <TextBlock Text="{Binding SelectedModule.Name}" FontSize="22" FontWeight="Bold"/>
                            <TextBlock Text="{Binding SelectedModule.Description}" Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" MaxWidth="520"/>
                        </StackPanel>
                        <Border Background="{StaticResource AccentBrush}" CornerRadius="6" Padding="10,6" VerticalAlignment="Center" Margin="16,0,0,0">
                            <TextBlock Text="运行" FontWeight="Bold"/>
                        </Border>
                    </StackPanel>
                </Border>

                <Border Grid.Row="1" Background="{StaticResource PanelBackground}" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="1" Margin="12" CornerRadius="8" Padding="12">
                    <ContentControl x:Name="ModuleContent"/>
                </Border>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
