<UserControl x:Class="ZenithKit.App.Views.ClipboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Border Background="{StaticResource PanelBackground}" Padding="12" CornerRadius="8" BorderBrush="{StaticResource BorderBrushColor}" BorderThickness="1">
        <StackPanel>
            <TextBlock Text="剪贴板历史" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
            <ListBox ItemsSource="{Binding Items}" MinHeight="260">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="0,0,0,8">
                            <TextBlock Text="{Binding Text}" TextWrapping="Wrap" Foreground="{StaticResource TextPrimary}"/>
                            <TextBlock Text="{Binding Timestamp}" Foreground="{StaticResource TextMuted}" FontSize="11"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Border>
</UserControl>
