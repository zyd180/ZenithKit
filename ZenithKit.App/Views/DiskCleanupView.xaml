<UserControl x:Class="ZenithKit.App.Views.DiskCleanupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="12">
            <StackPanel>
                <TextBlock Text="硬盘清理" Foreground="#fff" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal">
                    <Button Content="扫描" Width="80" Command="{Binding ScanCommand}"
                            IsEnabled="{Binding IsScanning, Converter={StaticResource InvertBool}}" Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding TotalSizeText}" Foreground="#4ec9b0" FontSize="14"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Category list -->
        <ListBox Grid.Row="1" ItemsSource="{Binding Categories}" Margin="12,0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 Background="Transparent" BorderBrush="#2a2f3a">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8,6" Margin="0,0,0,2" Background="#1e222d" CornerRadius="4">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <CheckBox IsChecked="{Binding IsSelected}" Margin="0,0,8,0"
                                      IsEnabled="{Binding DataContext.IsCleaning, RelativeSource={RelativeSource AncestorType=ListBox}, Converter={StaticResource InvertBool}}"/>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Name}" Foreground="#e8e8e8" FontSize="14"/>
                                <TextBlock Text="{Binding Path}" Foreground="#7d8291" FontSize="11"
                                           TextTrimming="CharacterEllipsis"/>
                            </StackPanel>
                            <TextBlock Grid.Column="2" Text="{Binding SizeText}" Foreground="#b0b3bc"
                                       FontSize="13" VerticalAlignment="Center" Margin="8,0,0,0"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Progress bar -->
        <ProgressBar Grid.Row="2" Value="{Binding ProgressValue}" Maximum="100" Height="4" Margin="12,6"
                     Visibility="{Binding IsCleaning, Converter={StaticResource BoolToVisibility}}"
                     Foreground="#3d6af2" Background="#1e222d"/>

        <!-- Footer -->
        <Border Grid.Row="3" Padding="12">
            <StackPanel Orientation="Horizontal">
                <Button Content="清理选中项" Width="100" Command="{Binding CleanCommand}"
                        IsEnabled="{Binding IsCleaning, Converter={StaticResource InvertBool}}"
                        Visibility="{Binding HasResults, Converter={StaticResource BoolToVisibility}}"
                        Margin="0,0,8,0"/>
                <TextBlock Text="{Binding StatusText}" Foreground="#7d8291" FontSize="12"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
