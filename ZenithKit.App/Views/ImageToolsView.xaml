<UserControl x:Class="ZenithKit.App.Views.ImageToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Height="240">
    <Grid Background="#1e1e1e">
        <Border Padding="12">
            <StackPanel>
                <TextBlock Text="图片转换/压缩" Foreground="#fff" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal">
                    <TextBox Text="{Binding SourcePath, UpdateSourceTrigger=PropertyChanged}" Width="320" Margin="0,0,0,6"/>
                    <Button Content="浏览" Width="80" Command="{Binding BrowseCommand}" Margin="6,0,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <StackPanel.Resources>
                        <Style TargetType="FrameworkElement">
                            <Setter Property="Margin" Value="0,0,6,0"/>
                        </Style>
                    </StackPanel.Resources>
                    <ComboBox Width="100" SelectedItem="{Binding Format}">
                        <ComboBoxItem Content="png"/>
                        <ComboBoxItem Content="jpg"/>
                        <ComboBoxItem Content="bmp"/>
                    </ComboBox>
                    <TextBox Text="{Binding MaxWidth}" Width="80"/>
                    <TextBox Text="{Binding MaxHeight}" Width="80"/>
                    <TextBox Text="{Binding Quality}" Width="80"/>
                    <Button Content="转换" Width="80" Command="{Binding ConvertCommand}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,6,0,0">
                    <TextBox Text="{Binding SaveDirectory, UpdateSourceTrigger=PropertyChanged}" Width="320" />
                    <Button Content="输出路径" Width="90" Command="{Binding BrowseSaveDirectoryCommand}" Margin="6,0,0,0"/>
                </StackPanel>

                <TextBlock Foreground="#777" FontSize="12" Text="格式/宽/高/质量(0-100，jpeg有效)" Margin="0,6,0,6"/>
                <TextBlock Text="{Binding ResultPath}" Foreground="#ccc" FontSize="12" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding Status}" Foreground="#7d8291" FontSize="11" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
