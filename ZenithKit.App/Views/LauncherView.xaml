<UserControl x:Class="ZenithKit.App.Views.LauncherView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Height="200">
    <Grid Background="#1e1e1e">
        <Border Padding="12">
            <StackPanel>
                <TextBlock Text="启动器" Foreground="#fff" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBox Text="{Binding Target, UpdateSourceTrigger=PropertyChanged}" Width="260" Margin="0,0,0,6"/>
                <Button Content="运行" Width="80" Command="{Binding LaunchCommand}"/>
                <TextBlock Foreground="#777" FontSize="12" TextWrapping="Wrap" Margin="0,8,0,0" Text="输入可执行路径、URL 或命令，使用系统 Shell 启动"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
