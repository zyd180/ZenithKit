<UserControl x:Class="ZenithKit.App.Views.PdfToolsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" Height="260">
    <Grid Background="#1e1e1e">
        <Border Padding="12">
            <StackPanel>
                <TextBlock Text="PDF 合并" Foreground="#fff" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal">
                    <StackPanel.Resources>
                        <Style TargetType="FrameworkElement">
                            <Setter Property="Margin" Value="0,0,6,0"/>
                        </Style>
                    </StackPanel.Resources>
                    <TextBox x:Name="AddPathBox" Width="260"/>
                    <Button Content="添加" Width="80" Click="AddPath_Click"/>
                    <Button Content="合并" Width="80" Command="{Binding MergeCommand}"/>
                </StackPanel>
                <ListBox ItemsSource="{Binding MergeList}" Height="120" Margin="0,6,0,6"/>
                <TextBlock Text="{Binding MergeResult}" Foreground="#ccc" FontSize="12" TextWrapping="Wrap"/>

                <Separator Margin="0,12,0,8"/>
                <TextBlock Text="PDF 拆分" Foreground="#fff" FontSize="16" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBox Text="{Binding SplitSource, UpdateSourceTrigger=PropertyChanged}" Width="320" Margin="0,0,0,6"/>
                <TextBox Text="{Binding SplitOutput, UpdateSourceTrigger=PropertyChanged}" Width="320" Margin="0,0,0,6"/>
                <Button Content="拆分" Width="80" Command="{Binding SplitCommand}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
